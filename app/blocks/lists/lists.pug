mixin ol-list()
  ol.list&attributes(attributes)
    block

mixin ol-list-multilevel()
  ol.list-multilevel&attributes(attributes)
    block

mixin ul-list()
  ul.list&attributes(attributes)
    block

mixin dl-list
  dl.list&attributes(attributes)
    block
