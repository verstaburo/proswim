mixin breadcrumbs(json)
  +b.UL.breadcrumbs&attributes(attributes)
    each crumb, index in json
      +e.LI.item
        +link(href='#')
          =crumb
        if index + 1 < json.length
          +icon('Arrow icon (bread crumbs)').icon
