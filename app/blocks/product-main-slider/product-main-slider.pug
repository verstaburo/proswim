mixin product-main-slider(images, alt, hasButtons, zoom)
  +b.product-main-slider&attributes(attributes).swiper-container.js-product-main-slider
    .swiper-wrapper
      each image in images
        - var imageClasses = [zoom ? 'js-product-main-slider-zoom' : '']
        - var videoClasses = [image.isVideo ? 'product-main-slider__slide_video' : '']
        - var rotateClasses = [image.isRotate ? 'product-main-slider__slide_rotate' : '']
        .swiper-slide.product-main-slider__slide(class=image.isVideo ? videoClasses.join(' ') : image.isRotate ? rotateClasses.join(' ') : imageClasses.join(' '))
          if image.isVideo
            video(src=image.src)
          if image.isRotate
            +e.A(href='#img-rotate-popup').smth.js-slider3D
              img(src=src=`content-img/product-cards/${image.src}.jpg` alt=alt || '@@')
            +e.SPAN.__rotate 360
          else
            img(src=src=`content-img/product-cards/${image.src}.jpg` alt=alt || '@@')
            if zoom
              +icon('Zoom icon').__zoom

    if hasButtons
      +e.buttons
        +e.button._prev
          +icon('Arrow 1')
        +e.button._next
          +icon('Arrow 1')




mixin product-main-slider-preview(images, alt, hasButtons, zoom)
  +b.product-main-slider_preview&attributes(attributes).swiper-container.js-product-main-slider
    .swiper-wrapper
      each image in images
        - var imageClasses = [zoom ? 'js-product-main-slider-zoom' : '']
        - var videoClasses = [image.isVideo ? 'product-main-slider__slide_video' : '']
        - var rotateClasses = [image.isRotate ? 'product-main-slider__slide_rotate' : '']
        .swiper-slide.product-main-slider__slide(class=image.isVideo ? videoClasses.join(' ') : image.isRotate ? rotateClasses.join(' ') : imageClasses.join(' '))
          if image.isVideo
            video(src=image.src)
          if image.isRotate
            +e.A(href='#img-rotate-popup').smth.js-slider3D
              img(src=src=`content-img/product-cards/${image.src}.jpg` alt=alt || '@@')
            +e.SPAN.__rotate 360
          else
            img(src=src=`content-img/product-cards/${image.src}.jpg` alt=alt || '@@')
            if zoom
              +icon('Zoom icon').__zoom

    if hasButtons
      +e.buttons
        +e.button._prev
          +icon('Arrow 1')
        +e.button._next
          +icon('Arrow 1')
