mixin searchbar(placeholder)
  +b.FORM.searchbar(method="POST" action="/")&attributes(attributes)
    +input(placeholder=placeholder).searchbar__input
    +e.BUTTON.button(type="submit")
      +icon('Search icon').icon
