mixin password-recovery-popup(isPage)
  +popup(class=isPage ? '_page' : '' id=!isPage ? 'password-recovery-popup' : '').password-recovery-popup
    +popup-header('Восстановить пароль')
    +popup-content.password-recovery-popup__content
      +e.subheading Введите E-mail на который #[br]был зарегистрирован аккаунт ProSwim.ru
      +e.FORM.form.__grid(method='post' action='/' data-parsley-validate data-parsley-trigger="focusout")
        +col(12)
          +input('Email адрес')(type='email' required)._size_md
        +col(12)
          +button.BUTTON(type='submit')._size_big._block Выслать инструкцию
