mixin articles-section(items)
  +section._grey.articles-section
    .container
      +section-head('Статьи')
      +section-content
        .grid
          each item in items
            +col(3)
              +article-card(item)
      +section-button
