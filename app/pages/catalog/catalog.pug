extends ../../layouts/default

block head
  - var pageTitle = 'Proswim - Очки для плавания'

block content
  +b.page
    .container
      +e.head
        +e.headcol._flexible
          +breadcrumbs(['Главная', 'Очки', 'Очки для плавания'])

      +grid
        +col(3)
        +col(9)
          +catalog-header('Очки для плавания', '174 товара')

block popups
