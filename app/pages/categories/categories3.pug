extends ../../layouts/default

block head
  - var pageTitle = 'Proswim - Для открытой воды'

block navbar
  +navbar

block content
  +b.page
    .container
      +e.head
        +e.headcol._flexible
          +breadcrumbs(['Главная', 'Для открытой воды'])
      +e.head._subhead
        +e.headcol._flexible
          +e.H1.heading Для открытой воды

      +categories-section(getData('categories3'), 6, true, true)

block popups
  +login-popup
  +registration-popup
  +password-recovery-popup
  +registration-complete-popup
