extends ../../layouts/default

block head
  - var pageTitle = 'Proswim - registration-complete'

block content
  +b.page
    .container
      +e.head
        +e.headcol._flexible
          +breadcrumbs(['Главная', 'registration-complete'])
      +e.head._subhead
        +e.headcol._flexible
          +e.H1.heading registration-complete

block popups